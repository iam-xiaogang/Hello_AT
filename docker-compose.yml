services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      TOOLBOX_CORS_ORIGINS: '["http://localhost:5173", "http://localhost:8080"]'

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
